"""
Attendance API — Query + Explicit Checkout Endpoints

GET    /api/attendance              All attendance (filterable by date)
GET    /api/attendance/today        Today's attendance
POST   /api/attendance/{id}/checkout  Explicit checkout for employee
"""
