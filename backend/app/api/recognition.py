"""
Recognition API — Engine Control Endpoints

POST   /api/recognition/start    Start recognition engine thread
POST   /api/recognition/stop     Stop recognition engine thread
GET    /api/recognition/status   Engine state + stats (FPS, tracks, etc.)
"""
